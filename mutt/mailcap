#text/html; links -dump -html-numbered-links 1 -html-images 1 -html-assume-codepage %{charset} %s; copiousoutput; nametemplate=%s.html
text/html; ~/.mutt/mail_html.sh %{charset} %s; copiousoutput; nametemplate=%s.html
#text/html; lynx -dump -force_html -assume_charset %{charset} -localhost %s; copiousoutput; nametemplate=%s.html
text/*; ~/.mutt/save_attachement.sh %s
application/*; ~/.mutt/save_attachement.sh %s
image/*; ~/.mutt/save_attachement.sh %s
audio/*; ~/.mutt/save_attachement.sh %s
